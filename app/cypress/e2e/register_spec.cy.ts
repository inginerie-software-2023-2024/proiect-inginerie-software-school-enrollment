describe("Register", () => {
  before(() => {
    // Delay to ensure server is ready
    cy.wait(2000)
  })
  it("user can register", () => {
    // user visits the register page
    cy.visit("/")
    // user enters register details
    //user clicks register button
  })
})
